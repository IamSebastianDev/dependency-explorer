# Changesets

Run `bun run changeset` when a user-facing change should be included in the next release.

Changeset files describe the affected package, release level, and changelog entry. `bun run version-packages` consumes them and updates package versions and changelogs.
